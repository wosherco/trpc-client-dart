name: trpc_client
description: A minimal trpc client for Dart and Flutter.
version: 0.1.0
repository: https://github.com/polvallverdu/trpc_client

environment:
  sdk: ^3.4.0

dependencies:
  http: ^1.2.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
